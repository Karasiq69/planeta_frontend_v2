import NewCarPageWrapper from "@/features/cars/components/NewCarPageWrapper";

type Props = {};
const Page = (props: Props) => {
    return (
        <NewCarPageWrapper/>
    );
};
export default Page;
